# dependencies
node_modules

# build output
dist
dist-ssr

# local env / editor / OS
*.local
.DS_Store
.vite

# logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# local tooling (absolute machine paths, not portable)
.claude

# local-only: pre-optimization source video (superseded by all-intra film-wrap.mp4)
public/generated/film-wrap-source-longgop.mp4
